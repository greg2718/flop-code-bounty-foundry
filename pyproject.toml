[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "flop-code-bounty-foundry"
version = "0.1.0"
description = "Turn software issues into paid paper-FLOP jobs on FLOP Work Exchange."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "Greg Smith" }]
dependencies = [
    "cryptography>=42,<51",
    "PyYAML>=6.0,<7",
    "flop-work-exchange @ git+https://github.com/greg2718/flop-work-exchange.git@main",
]

[project.optional-dependencies]
dev = [
    "mypy==1.19.1",
    "pytest==8.4.2",
    "ruff==0.16.5",
    "types-PyYAML>=6.0,<7",
]

[project.scripts]
flop-code-bounty-foundry = "flop_code_bounty_foundry.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
flop_code_bounty_foundry = ["data/*.json", "py.typed"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "S"]
ignore = ["S101"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["flop_code_bounty_foundry"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "live: optional tests that require local Scout/Bench/Router/Sentinel installs",
]
